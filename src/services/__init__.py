"""Service layer: business capabilities behind the UI.

Phase 1 ships only the theme service. Later phases add captioning,
transcription and export services here — the UI will keep using these
interfaces, never talking to processing internals directly.
"""
